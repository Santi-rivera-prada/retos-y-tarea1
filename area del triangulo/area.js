function calcularAreaTriangulo(base, altura) {
    return (base * altura) / 2;
  }

  //la base y la altura es preferencial 
  const base = "5";
  const altura = "4";
  const area = calcularAreaTriangulo(base, altura);
  console.log(area); 
  